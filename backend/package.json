{
  "name": "u-windows-backend",
  "version": "0.1.0",
  "private": true,
  "description": "Cerebro (backend) del asistente Ü para Windows: computer-use + MCP + aprendizaje. Desplegable en Vercel, separado del cliente.",
  "type": "module",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "build": "tsc --noEmit",
    "dev": "vercel dev",
    "deploy": "vercel deploy --prod"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {},
  "devDependencies": {
    "@vercel/node": "^3.2.0",
    "typescript": "^5.5.4"
  }
}
